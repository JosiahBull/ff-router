name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  # All merge gates live in the reusable checks workflow so release.yml can run
  # the identical suite (see .github/workflows/checks.yml).
  checks:
    uses: ./.github/workflows/checks.yml
