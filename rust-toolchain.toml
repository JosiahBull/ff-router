# Nightly is required for the size-optimised `build-std` packaging build
# (see scripts/build.sh, which also provisions the `rust-src` component).
[toolchain]
channel = "nightly"
components = ["clippy", "rustfmt"]
